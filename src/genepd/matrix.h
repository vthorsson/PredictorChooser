#ifndef __MATRIX_H__
#define __MATRIX_H__

#include <stdlib.h>
#include <stdio.h>

int mat_prod(int, int **, int **, int **);
void mat_or(int, int **, int **, int **);
void printMatrix(int **, int, int);

#endif
